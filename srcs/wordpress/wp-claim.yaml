apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wp-claim
  labels:
    run: wordpress
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 600Mi